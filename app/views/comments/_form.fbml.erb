<%= fb_name current_user, :use_you=>false %>
<%= @user.id %>
<% form_for :comment, :url => item_comments_path(@item) do |f| %>
  <p><%= f.text_field :body, :label => "" %></p>
  <p><%= f.submit "Add comment" %></p>
<% end %>
