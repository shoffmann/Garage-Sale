<li>
	<p>
		<%= link_to("Delete", {:action => "destroy", :id => @comment },
				:confirm => "Are you sure you want to delete this comment?", :method => :delete) %>
		<%= comment.user_id %> 
		posted <%= comment.created_at.strftime('%l:%M %p') %>
		on <%= comment.created_at.strftime('%B %d, %Y') %>
	</p>
	<p><%= comment.body %></p>
</li>