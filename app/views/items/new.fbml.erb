<h1>Create a New Item</h1>

<% form_for(@item, :canvas => false, 
						:html => { :multipart => true, :promptpermission=>"email,publish_stream" }) do |f| %>
	<%= render :partial => "form", :locals => {:f => f} %>
	<%= f.submit "Add" %>
<% end %>
